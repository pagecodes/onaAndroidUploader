# onaAndroidUploader
connect to AWS S3 using python, to upload APKs that are built in Ona. Should contain .devcontainer so we have a reproducible Android SDK container solution that allows us to add AWS keys and upload APKs built from a second git repo
